# Copy to .env and fill in your credentials — never commit .env
SIMLA_STORE=mystore          # subdomain: "mystore" → mystore.simla.com
SIMLA_API_KEY=your_api_key_here
SIMLA_GRAPHQL_TOKEN=your_graphql_bearer_token_here   # optional
